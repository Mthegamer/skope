using UnityEngine;
using System.Collections;

[RequireComponent (typeof(AudioSource))]

public class MovieNGUI : MonoBehaviour {
/*
	public MovieTexture movTexture;
	public UITexture UItex;

	// Use this for initialization
	void Start () {
		UItex.material.mainTexture = movTexture as MovieTexture;
		audio.clip = movTexture.audioClip;

		
	}

	public void ToggleMovie(bool play){
		if(play){
			if(!movTexture.isPlaying){
				movTexture.Play();
				audio.Play();
			}
		}

		else{
			if(movTexture.isPlaying){
				movTexture.Stop();
			}
		}
	}

	public void PlayMovie(){
		if(!movTexture.isPlaying){
			movTexture.Play();
			audio.Play();
		}
	}

	public void StopMovie(){
		if(movTexture.isPlaying){
			movTexture.Stop();
		}

	}
	*/
}
